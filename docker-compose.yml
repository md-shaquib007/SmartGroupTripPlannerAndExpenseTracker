version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: tripsync-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tripsync
      MYSQL_USER: tripsync_user
      MYSQL_PASSWORD: tripsync_pass
    ports:
      - "3306:3306"
    volumes:
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: tripsync-app
    ports:
      - "8080:8080"
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: tripsync
      DB_USER: tripsync_user
      DB_PASS: tripsync_pass
    depends_on:
      db:
        condition: service_healthy
